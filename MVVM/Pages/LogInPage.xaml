<Page x:Class="HealthTrackerMVVM.Pages.LogInPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HealthTrackerMVVM.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LogInPage" Background="#121212" Foreground="White">
    
    <Grid>
        <Border Background="#1E1E1E"
            Width="320"
            Padding="30"
            CornerRadius="15"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="🔐 Welcome Back"
                       FontSize="26"
                       FontWeight="Bold"
                       Margin="0 0 0 20"
                       HorizontalAlignment="Center" />

                <TextBlock Text="Username" FontSize="14" Margin="0 5 0 2"/>
                <TextBox x:Name="usernameBox"
                     Height="35"
                     FontSize="14"
                     Padding="8"
                     Background="#2C2C2C"
                     Foreground="White"
                     BorderThickness="0"/>

                <TextBlock Text="Password" FontSize="14" Margin="10 15 0 2"/>
                <PasswordBox x:Name="passwordBox"
                         Height="35"
                         Padding="8"
                         Background="#2C2C2C"
                         Foreground="White"
                         BorderThickness="0"/>
                
                <Button x:Name="login"
                    Content="Log In"
                    Click="LogIn_Clicked"
                    Height="40"
                    Margin="0 20 0 0"
                    FontWeight="SemiBold"
                    Background="#03DAC6"
                    Foreground="#000"/>

                <Button Content="← Go Back"
                    Click="GoBack_Clicked"
                    Height="35"
                    Background="Transparent"
                    Foreground="White"
                    BorderBrush="#03DAC6"
                    BorderThickness="1"
                    Margin="0 5 0 0"
                    FontWeight="Normal"/>
            </StackPanel>
        </Border>
    </Grid>
    
</Page>
